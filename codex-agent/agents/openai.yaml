interface:
  display_name: "Codex Agent"
  short_description: "Run and resume Codex CLI workers"
  default_prompt: "Use $codex-agent to delegate this task through Codex CLI and preserve the session ID for follow-up work."
